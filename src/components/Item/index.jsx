import React from 'react'

export default function index({item, item2, item4}) {
  return (
    <div>
      <div>
        <p>{item}</p>
        <p>{item2}</p>
        <p>{item4}</p>
      </div>
    </div>
  )
}
